# Anonymous

**익명 자식 객체:** 자식클래스가 재사용되지않고 오로지 특정 위치에서 사용되는 경우

*ex)*

```java
Parent [필드 변수] = new Parent Class (매개값, ...){
//필드
//메소드
};
```

**익명 구현 객체:** 구현 객체 클래스가 재사용되지않고 오로지 특정 위치에서 사용되는 편리

*ex)*

```java
Interface [필드 변수] = new Interface(){
//인터페이스에 선언된 추상 메소드의 실체 메소드 선언
//필드
//메소드
};
```
